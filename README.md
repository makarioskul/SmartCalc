# Калькулятор v4 (с историей)
Добавлена запись истории вычислений в файл history.txt.
Меню интерактивное, поддерживаются все предыдущие операции.

Сборка:
```bash
g++ main.cpp -o calc
```

Запуск:
```bash
./calc
```
